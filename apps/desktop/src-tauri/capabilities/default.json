{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "log:default",
    "dialog:default",
    {
      "identifier": "opener:allow-open-path",
      "comment": "Gate 3 opens the exported PDF/EPUB in the user's default reader. `opener:default` covers reveal-in-folder but not open-path, and the scope has to be broad because the user picks the export folder themselves.",
      "allow": [{ "path": "**" }, { "path": "/**" }]
    }
  ]
}
